import { NutritionBenefitsPipe } from './nutrition-benefits.pipe';

describe('NutritionBenefitsPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionBenefitsPipe();
    expect(pipe).toBeTruthy();
  });
});
